import 'package:flutter/material.dart';

class TimeAttackCameraPageView extends StatelessWidget {
  const TimeAttackCameraPageView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
